" important, this is the encoding your vim supports
set encoding=utf-8
" fileencoding sets the option to write the file
" fileencodings lists encodings for vim to try when opening file

" only usefull for systemwide vimrc, this overrides -C flag
set nocompatible

" how many spaces does a tab display as
set tabstop=2

" this tells vim to always use the tabstop value
set shiftwidth=0

" turn on incremental search
"set incsearch

syntax on
set number
set hlsearch
set ai
set ruler
set colorcolumn=80
set background=dark
